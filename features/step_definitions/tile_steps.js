const { Before, Given, When, Then } = require('cucumber');

Given('memory-app is loaded', function() {

})

When('tile at {int},{int} is pressed', function (int, int2){
  return 'pending';
})

Then('tile at {int},{int} should display word', function (int, int2){
  return 'pending';
})
